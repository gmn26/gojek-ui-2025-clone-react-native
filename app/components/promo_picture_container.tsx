import { TouchableOpacity, View } from "react-native";

const PromoPictureContainer = () => {
  return (
    <View className="w-full my-2 px-4">
      <TouchableOpacity className="h-40 bg-slate-300 rounded-2xl" />
    </View>
  );
};

export default PromoPictureContainer;
